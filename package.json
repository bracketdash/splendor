{
  "name": "next-app",
  "version": "0.1.0",
  "scripts": {
    "dev": "node src/trainer/prep && next dev",
    "trainer": "node src/trainer/prep --modulize && node src/trainer/trainer",
    "build": "node src/trainer/prep && next build",
    "start": "node src/trainer/prep && next start",
    "export": "node src/trainer/prep && next build && next export"
  },
  "dependencies": {
    "next": ">=12.1.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "eslint": "7.30.0",
    "eslint-config-next": "11.0.1"
  }
}